// var randoms = Math.random() * 8;
// var output = Math.round(randoms);
// console.log(output);

for (let i = 0; i <= 5; i++) {
  var random = Math.random() * 5;
  var output = Math.round(random);
  console.log(output);
}
